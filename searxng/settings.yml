use_default_settings: true

server:
  bind_address: "0.0.0.0"
  port: 8081
  secret_key: "${SEARXNG_SECRET}"
  method: "POST"
  limiter: false
  image_proxy: true

search:
  safe_search: 0
  autocomplete: ""
